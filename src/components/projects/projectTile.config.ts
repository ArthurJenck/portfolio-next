// Opacité des couches de la pile décorative derrière l'image (une par profondeur).
export const STACK_OPACITY_STEP = 0.2

// Décalage en Z des couches de la pile, aux deux temps du keyframe GSAP.
export const STACK_Z_NEAR = 8
export const STACK_Z_FAR = 20

// Un tap est une pression brève avec peu de mouvement ; au-delà, c'est un drag ou un hold.
export const TAP_MOVEMENT_THRESHOLD_PX = 10
export const LONG_PRESS_MS = 300
