import SectionTitle from "@/components/SectionTitle"

const Projets = () => {
    return (
        <section id="projets">
            <SectionTitle title="Projets" />
            <div>{/* TODO: PROJECTS CAROUSEL */}</div>
        </section>
    )
}

export default Projets
